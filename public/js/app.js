'use strict';

import React             from 'react';
import ReactDOM          from 'react-dom';
import TreeMenuComponent from './components/tree-menu';

let $container = document.getElementById('container');

ReactDOM.render(
    <TreeMenuComponent />
, $container);
